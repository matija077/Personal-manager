import  HomePage from './home.page';

type HomeContainerPropsType = {
    children: []
};

function HomePageContainer(props: HomeContainerPropsType) {

    return (
        <HomePage>

        </HomePage>
    );
}

export default HomePageContainer;